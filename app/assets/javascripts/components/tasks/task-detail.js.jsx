(function(root) {
  'use strict';

  root.TaskDetail = React.createClass({
    render: function () {
      return (
        <div className="task-detail">
          This is the task detail view.
        </div>
      );
    }
  });
}(this));
