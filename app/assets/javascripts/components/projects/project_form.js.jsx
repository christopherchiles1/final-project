(function(root) {
  'use strict';

  root.ProjectForm = React.createClass({
    render: function () {
      return (
        <div className="project-form">
        </div>
      );
    }
  });

}(this));
