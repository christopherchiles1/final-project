(function(root) {
  'use strict';

  root.ProjectsIndex = React.createClass({
    render: function () {
      return (
        <ul className="projects-index">
        </ul>
      );
    }
  });

}(this));
